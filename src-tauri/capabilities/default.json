{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the main window is allowed to do. Tauri v2 grants nothing by default, so an unlisted command fails at runtime rather than at build time — which is how a button that looked fine did nothing at all.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/docmap",
          "sidecar": true
        }
      ]
    }
  ]
}
