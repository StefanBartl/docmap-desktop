# Handover — 2026-08-20

Where the work stands and what the next session should pick up. Written at
the end of a long run rather than reconstructed afterwards, so the reasons
are here and not only the outcomes.

Three repositories, all clean and all pushed to `main`:
`documentation.nvim`, `docmap-desktop`, `runtime-analysis.nvim`.

## Table of content

- [What shipped](#what-shipped)
- [Where to continue — the ranked list](#where-to-continue--the-ranked-list)
- [The afternoon run — 2026-08-20](#the-afternoon-run--2026-08-20-1338-to-now)
- [Decisions taken that the next session must not re-litigate](#decisions-taken-that-the-next-session-must-not-re-litigate)
- [Ideas recorded, nothing built](#ideas-recorded-nothing-built)
- [Things that need you, not the next agent](#things-that-need-you-not-the-next-agent)
- [How to run everything](#how-to-run-everything)

---

## What shipped

**Fourteen language backends, taking the engine from nine to twenty-three.**
Wave 1 complete — Python, C#, Go, Rust, PHP, Ruby, Kotlin, Swift, Dart,
Scala — and four of wave 2: Haskell, Elixir, Erlang, OCaml.

Each one has a backend, a spec, a grammar in the release script, a row in
`README.md`'s language table, and an entry in
`docs/ROADMAP/IDEAS/MULTILANG.md` recording what it cost and what it taught.

**The single most useful thing to carry forward:** every one of the fourteen
was measured against a real repository, and **every measurement changed
something**. Not one exception. Go's test functions inflated the public API
two and a half times; Rust's workspace crates would have resolved one
member's import to another member's file; C#'s `#if` blocks hid ten
functions; Python's typed splats vanished from every annotated signature;
OCaml documented *below* its declarations and the first version found 0 of
50. Fixtures caught the rest. **A backend that has not been run against
somebody else's code is not finished.**

Also shipped, outside the languages:

- **Assembly** (the ninth backend, before the fourteen) — the first with no
  tree-sitter grammar, by design.
- **Doc coverage per language**, which immediately exposed that the measure
  was unfair to languages with no per-parameter convention, and the
  `param_docs` contract field that fixed it.
- **`Project → Check exactly`** in the app — the answer the staleness mark
  only approximates.
- **Sort by "least recently generated"**, the question staleness cannot ask.
- **The placeholder and context-note translations**, plus structural tests
  that stop the next literal getting in.
- **`docs/USAGE.md` rewritten** from an inventory of the code, and
  `documentation.nvim`'s README given the `## Languages` section it never had.

## Where to continue — the ranked list

The full inventory across all three repositories, ordered by effort rather
than by topic, is [`PLAN.md`](PLAN.md) — written 2026-08-20. What follows
here is the narrower question of what this run left behind.

**Done — `documentation.nvim`'s remaining docs (`§10.7`), 2026-08-20**,
commit `39a6813`. It was the highest-value item and the inventory said why:
`PIPELINE.md` — "the document to read before changing anything", 2158 lines
— did not mention multi-language support once, and `REUSE.md` told readers
the one precondition was `---@module` on their files, false for
twenty-two of the twenty-three. Two new files close it:
`documentation.nvim/docs/LANGUAGES.md`, the per-language reference that
existed nowhere, tabulated from `lang_registry.report()` rather than from
prose; and `docs/README.md`, the folder index it never had. **Read
`LANGUAGES.md` before touching anything language-shaped** — it is now the
reference, and `MULTILANG.md` is the record.

**Done — the parity pass (`§12`), 2026-08-20**, commit `c9b497a`. The
matrix is `documentation.nvim/docs/LANGUAGES.md § Parity`, measured by
`scripts/parity.lua` over one fixture per language rather than written by
hand. Four columns are complete across all twenty-three — file summary,
declaration summary, visibility, require edges — and markers too, after the
pass fixed three languages where they were not.

**The two findings to carry forward**, both of which the audit existed to
surface and neither of which is visible from any one language:

- **Call edges exist in four backends of twenty-three** — `lua`, `js`, `ts`,
  `tsx`. The other nineteen return nothing, so Calls, Module Calls,
  `:DocMap why`, the call-hierarchy view and `dead-function`'s call tier are
  all empty for a Go, Rust or Python project. **Nothing in those languages
  makes it impossible; it is unbuilt.** This is the largest single gap in
  the tool.
- **Module-scope symbols are missing in `zig`, `java`, `c`, `cpp`** — the
  four oldest non-Lua backends. Every one written after them has them.

Both are in the engine's `ROADMAP.md`, described as unbuilt rather than as
impossible. **The app's own follow-up is smaller and separate** (§12 in
`WORKPLAN.md`): a panel empty because the language has no extraction yet
should say so, the way the Types panel already names its missing tool.

**Four defects fixed on the way**, all the same shape — a capability
reporting nothing rather than reporting a problem. Markers were invisible
inside Haskell `haddock`, Kotlin `multiline_comment` and Dart
`documentation_comment` nodes, which is to say inside the *doc* comment of
each of those languages. A block comment's closer was being reported as part
of the note. And OCaml's visibility silently reads as "everything public" on
a host missing the `ocaml_interface` grammar — not a code defect, a trap now
written down.

> **One correction the docs pass produced, and it belongs here because this
> file repeated it:** `param_docs = false` is **nine** languages, not eight,
> and **fourteen** judge parameters, not fifteen. Ruby was described in
> `MULTILANG.md` as "a different case again" — parsed and shown, not judged
> — and then left out of the total, where it belongs, because the field is
> `false` and the field is what every consumer reads. No number the tool
> reports was ever affected: `judges_params` is derived from the field, not
> from the sentence. Nothing caught it until `LANGUAGES.md` tabulated the
> registry instead of the prose. **A count written in a document is a claim;
> a count derived from the code is a fact.**

**Done — per-project settings (`docmap-desktop`), 2026-08-20**, commit
`7204426`, and **not** in Settings: that dialog is explicitly *this
machine's*, and one project's answer must not become every project's. Own
dialog, **Project → Project settings…**. The engine had neither option
until `documentation.nvim` `a2363fd` (`opts.exclude`, `opts.languages`,
`--exclude=`, `--languages=`); this app is their first consumer. The
language list is read from `--capabilities`, nothing ticked means all of
them, and the scope is looked up in Rust by root so `check_map` cannot
forget it and report a project stale forever.

**Done — the first real look at the window, 2026-08-20**, commit `54f4c41`.
Everything in the two rounds above shipped "verified structurally and by
test, not looked at in a running window". The first look found a defect in
the dialog directly above: at the app's minimum window height, **Save** sat
149px below the fold, in a box whose obvious wheel targets scroll something
else. Fixed by pinning a dialog's own action; the feedback dialog had it
too. `tools/preview/` is what found it — the real markup, CSS and `main.js`
in a browser with a stubbed Tauri bridge and the real 23-backend language
list, so layout can be measured. It proves layout, not behaviour.

**Decided — the `BUG:` marker question, 2026-08-20**, engine commit
`11dacc2`: **Quicks yes, `check.lua` no.** Every finding `check.lua` produces
is a measurement; a `FIX` marker is the author's claim about their own code.
Gating on one would render a claim like a measurement and fail the repository
that wrote its defect down while passing the one that kept quiet. So it is
counted — `recorded-defects`, `FIX` family only, `bad = 1`, and a `basis`
that says whose sentence it is — and nothing fails because of it.

**Still open. 1. Call edges for the other nineteen backends** — the largest single gap
in the tool, described above and recorded in the engine's `ROADMAP.md` as
unbuilt rather than impossible. Real per-language feature work, not a
cleanup.

**Not scheduled:** the remaining sixteen languages. See the decisions below.

## The afternoon run — 2026-08-20, 13:38 to now

Written as it happened rather than reconstructed. `documentation.nvim` at
`d995ca3`, `docmap-desktop` at `ff8d252`, both pushed, CI green 5/5.

### Shipped, in order

| | What | Where |
|---|---|---|
| **Q10/Q11** | Hover call-count badge; `report_style = "preview-tab"` | `9a1cd10`, engine + `runtime-analysis` |
| **Q9** | GitHub Action — adopting the check costs three lines | `3ae9c83` |
| **M1** | **Go call edges** — the fifth backend to emit them | `59b60cb` |
| — | CI builds the Go grammar, or `go_calls_spec.lua` was never a gate | `ce3482a` |
| **Q4/Q5** | `orphaned-class-alias`, `test-references-missing` | `838a394` |
| **M2** | `tag-require-missing`, `tag-file-unavailable` | `e254588` |
| **M8** | I18N-0: findings carry params, one catalog, **schema 5** | `1a8e948` |
| — | Doc sweep over the afternoon's own commits | `25914b1` |
| **D2** | Hierarchy gesture swap + *Classic clicks* toggle | `ed27425` |
| **D3/D4** | View brainstorm (`IDEAS.md` §2.5); the fifteen languages listed | `c5b6cee` |
| **Q13** | Extension API stage 1 — the compatibility statement | `3effda4` |
| **Q7** | Print stylesheet | `7de62fd` |
| **Q14** | `:checkhealth` language support | `d995ca3` |

### The findings that outlived their features

Each of these was worth more than the ticket that produced it, and each was
found by **measuring, never by reading**. They are listed because a future
session will be tempted to skip the measuring step.

1. **A Go package is a directory.** A file-scoped resolver loses nearly half
   a real Go call graph — `aws/smithy-go`, 397 of 883 edges cross files of
   one package. The general lesson for L1's remaining languages: *ask what a
   scope is in this language before writing its query.* Lua and the ECMA
   family make file and scope the same thing and taught nothing about it.
2. **Silence is not evidence.** `test-references-missing`'s first version
   anchored its require query on `(chunk …)` and matched **once** across 75
   spec files, because a spec here is `return function(H) … end`. It
   reported nothing because it looked at nothing. What proves a check works
   is the count of things it *resolved*, plus a positive control — never a
   clean run.
3. **A corpus that lacks a case is not evidence the case works.** The I18N-0
   formatter escaped `%` in values, which `gsub` needs for a replacement
   *string* and never for what a replacement *function* returns. 140 real
   findings across three repositories contained no percent sign, so only the
   dedicated spec caught it.
4. **Two stated preconditions were false.** M2's plan said "~30 repositories
   with a committed map"; every plugin here but `documentation.nvim`
   gitignores `docs/map/`. M8's said findings are in `module_map.json`; the
   artifact writer is an explicit whitelist and never carried them.
5. **`@see` is unused in this ecosystem** — 0, 0 and 4 targets across the
   three repos, all four resolving. That is why M2 checks *requires* rather
   than the `@see` its own entry proposed.

### Where to continue

`docmap-desktop/docs/PLAN.md` is the roadmap and is current — every entry
finished today is struck there, and the four Teil-0 decisions are answered.

Open Quick Wins: **Q6** (per-entry reference anchors) and **Q8** (`K` in the
browser) — the two that pair, since both use the reference registry. Then
the middle tier, where **M10** (the two `runtime-analysis` joins) is the
highest value per effort: no new extraction on either side.

### Two things a new session needs and will not guess

- **The Go grammar is not on the runtimepath.** Run the tests with
  `DOCMAP_GO_PARSER="C:/tools/docmap-grammars/go.dll"`, or `go_calls_spec`
  and `lang_go_spec` take their skip paths and pass without asserting
  anything.
- **The byte-compare technique for anything touching finding text.** Dump
  `severity|check|node|message` for three real repositories before the
  change and diff after. It is what proved I18N-0 changed nothing visible,
  and it is cheaper than reasoning about 24 templates. `lib.nvim` and
  `runtime-analysis.nvim` keep their specs in `docs/TESTS`, not `TESTS`.

## Decisions taken that the next session must not re-litigate

Recorded in full in `documentation.nvim`'s
`docs/ROADMAP/IDEAS/MULTILANG.md` § *Four decisions taken 2026-08-20*.

1. **Coverage per language reports two counts.** `summarised` (comparable
   across all twenty-three) and `documented` (comparable across the fourteen
   that judge parameters — nine declare `param_docs = false`; see the
   correction above). One figure per language would mix two measures.
2. **The interface default stays written out per backend.** Seven languages,
   seven explicit rules. A shared contract field was considered and declined
   — each language spells it differently and each has a different
   surrounding default.
3. **The interface-file question is answered per language.** OCaml's sibling
   `.mli`, C's per-file rule, Delphi's in-file split. They look alike and are
   not.
4. **Wave 4 is cancelled** — Fortran, Ada, COBOL, Delphi, MATLAB, VB.NET are
   not being built, and the rest of wave 2 plus waves 3 and 5 are *available*
   rather than scheduled. Scope, not difficulty. **They become buildable
   again the moment a user asks for one**, and the recorded decisions mean
   restarting costs a day rather than a rediscovery.

The standing instruction all four were decided under, worth keeping:
**rather more effort, correct and separable, than a shortcut that falls over
later.**

## Ideas recorded, nothing built

Four, each written where the plugin that owns it will look:

- **API traffic as a measurement** — `runtime-analysis.nvim` `IDEAS.md`
  §1.7b. Sizes, durations, retries-to-success and error taxonomy, in both
  directions. Metadata and shapes, **never payloads**, decided up front
  because the captures are committed. This is the step from counting to
  measuring, and the path to a profiler.
- **Compiler Explorer, two steps further** — `documentation.nvim` `IDEAS.md`
  §3.6. Compare two marked functions side by side in one `clientstate`, and
  offer a *local* Compiler Explorer. The committed page must never hard-code
  one machine's `localhost`.
- **Name the tool a missing panel needs** — `documentation.nvim` `IDEAS.md`
  §6.8. With one correction that matters: **the map's twenty checks are its
  own and need no linter in any language.** Only the Types panel is
  tool-dependent. A mason.nvim hint belongs in Neovim only.
- **User-supplied checklists, applied by an agent** — `documentation.nvim`
  `IDEAS.md` §6.9. Scoped to a language or to every project, run as a
  **report first**, applied only on a reader's choice, with an agent writing
  diffs through the MCP tools that already exist.

## Things that need you, not the next agent

- **`§9.3` — more views.** "Like Hierarchy but with another focus" is a
  direction, not a task. One named view with a question it answers beats
  three general ones.
- **`§9.2` — the gesture swap.** Single click leaves the Hierarchy view for
  the Tree. The *surprise* is treated (the hover names both gestures now);
  whether the two should trade places is a decision with existing muscle
  memory behind it.
- **GitHub Discussions**, if feedback should be a thread rather than an
  issue. One line per category in `feedback.rs`.

## How to run everything

```bash
nvim --headless -l scripts/ci.lua
```

in `documentation.nvim` — five gates. A docs change makes the map stale;
regenerate with `scripts/gen_map.lua` and commit the result.

The language specs skip when their grammar is absent, which is the normal
local state. To run them for real, point at the built grammars:

```bash
DOCMAP_PYTHON_PARSER=C:/tools/docmap-grammars/python.dll nvim --headless -u NONE -l TESTS/run.lua
```

Every backend spec reads its own `DOCMAP_<LANG>_PARSER` — the full list is in
`documentation.nvim/docs/LANGUAGES.md § Running the language specs`, along
with the four backends that have no variable because Neovim ships their
grammars. All twenty-three
grammars are built into `C:/tools/docmap-grammars/` on this machine, and
`scripts/build_engine_release.sh` builds them from source for a release —
**twenty-three files for twenty-two languages**, because OCaml needs two
(`.ml` and `.mli` are different languages to the parser) and assembly needs
none.

In `docmap-desktop`:

```bash
cd src-tauri && cargo test
```

```bash
node --test src/lib/*.test.js
```

`cargo test` needs the placeholder sidecar first — see `docs/HANDOVER.md`.

To look at the frontend without building the app:

```bash
python tools/preview/preview.py
```

Then open `http://localhost:8731/tools/preview/preview.html`. Real markup,
real CSS, real `main.js`; every `invoke` answered by `tools/preview/stub.js`.
Layout only — the commands do nothing, and a browser is not WebView2.
